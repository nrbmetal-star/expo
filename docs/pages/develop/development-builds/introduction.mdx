import React, { useState } from 'react';

export default function TelcoAssistApp() {
  // স্ক্রিন নেভিগেশন স্টেট: 'login', 'dashboard', 'settings'
  const [screen, setScreen] = useState('login');
  
  // লগইন স্টেট
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

  // সেটিংস স্টেট
  const [notifications, setNotifications] = useState(true);
  const [darkMode, setDarkMode] = useState(false);
  const [regionFilter, setRegionFilter] = useState('All');

  const cards = [
    { title: 'Active Sites', value: '24' },
    { title: 'Pending Materials', value: '08' },
    { title: 'Open Alarms', value: '05' },
    { title: 'Team Members', value: '16' },
  ];

  const sites = [
    { id: 'NWMOB1', status: 'Running', region: 'Dhaka' },
    { id: 'DHKCFI', status: 'Pending', region: 'Dhaka' },
    { id: 'DHSETI', status: 'Completed', region: 'Secretariat' },
  ];

  const actions = [
    'Generate Mail',
    'Material Request',
    'Access Letter',
    'Daily Report',
  ];

  // লগইন সাবমিট হ্যান্ডলার
  const handleLogin = (e) => {
    e.preventDefault();
    // এখানে যেকোনো ডামি ইমেইল ও পাসওয়ার্ড দিলেই ড্যাশবোর্ডে নিয়ে যাবে
    if (email && password) {
      setScreen('dashboard');
    }
  };

  // ১. লগইন স্ক্রিন ডিজাইন
  if (screen === 'login') {
    return (
      <div className="min-h-screen bg-gray-100 flex items-center justify-center p-4">
        <div className="bg-white rounded-3xl shadow-xl p-8 max-w-md w-full text-center space-y-6">
          <div>
            <h1 className="text-3xl font-bold text-black">TelcoAssist AI</h1>
            <p className="text-gray-500 mt-2">Telecom & IBS Work Management</p>
          </div>
          
          <form onSubmit={handleLogin} className="space-y-4 text-left">
            <div>
              <label className="text-sm font-semibold text-gray-600 block mb-1">User Email</label>
              <input 
                type="email" 
                required
                placeholder="name@telco.com" 
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                className="w-full bg-gray-50 border rounded-2xl p-4 outline-none focus:border-black transition"
              />
            </div>
            <div>
              <label className="text-sm font-semibold text-gray-600 block mb-1">Password</label>
              <input 
                type="password" 
                required
                placeholder="••••••••" 
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                className="w-full bg-gray-50 border rounded-2xl p-4 outline-none focus:border-black transition"
              />
            </div>
            <button 
              type="submit" 
              className="w-full bg-black text-white rounded-2xl p-4 font-semibold hover:bg-gray-800 transition mt-2"
            >
              Sign In
            </button>
          </form>
          <p className="text-xs text-gray-400">Authorized personnel only. RF Operations Division.</p>
        </div>
      </div>
    );
  }

  // ২. সেটিংস স্ক্রিন ডিজাইন
  if (screen === 'settings') {
    return (
      <div className="min-h-screen bg-gray-100 p-4">
        <div className="max-w-2xl mx-auto bg-white rounded-3xl shadow-lg p-6 mt-6 space-y-6">
          <div className="flex items-center justify-between border-b pb-4">
            <h2 className="text-2xl font-bold">Control Settings</h2>
            <button 
              onClick={() => setScreen('dashboard')} 
              className="bg-gray-100 hover:bg-gray-200 text-sm font-medium px-4 py-2 rounded-xl transition"
            >
              Back to App
            </button>
          </div>

          <div className="space-y-4">
            <div className="flex items-center justify-between p-4 bg-gray-50 rounded-2xl">
              <div>
                <h3 className="font-semibold">Push Notifications</h3>
                <p className="text-xs text-gray-500">Get alerts for critical alarms and material delivery</p>
              </div>
              <input 
                type="checkbox" 
                checked={notifications} 
                onChange={() => setNotifications(!notifications)}
                className="w-5 h-5 accent-black cursor-pointer"
              />
            </div>

            <div className="flex items-center justify-between p-4 bg-gray-50 rounded-2xl">
              <div>
                <h3 className="font-semibold">Dark Mode (Beta)</h3>
                <p className="text-xs text-gray-500">Switch app layout to high-contrast dark theme</p>
              </div>
              <input 
                type="checkbox" 
                checked={darkMode} 
                onChange={() => setDarkMode(!darkMode)}
                className="w-5 h-5 accent-black cursor-pointer"
              />
            </div>

            <div className="p-4 bg-gray-50 rounded-2xl space-y-2">
              <div>
                <h3 className="font-semibold">Default Operations Region</h3>
                <p className="text-xs text-gray-500">Filter your primary dashboard data</p>
              </div>
              <select 
                value={regionFilter} 
                onChange={(e) => setRegionFilter(e.target.value)}
                className="w-full bg-white border rounded-xl p-3 outline-none mt-2"
              >
                <option value="All">All Regions (Bangladesh)</option>
                <option value="Dhaka">Dhaka Division</option>
                <option value="Chittagong">Chittagong Division</option>
              </select>
            </div>
          </div>

          <div className="border-t pt-4 flex gap-3">
            <button 
              onClick={() => setScreen('login')} 
              className="w-full bg-red-50 hover:bg-red-100 text-red-600 font-semibold p-4 rounded-2xl transition"
            >
              Log Out
            </button>
          </div>
        </div>
      </div>
    );
  }

  // ৩. মূল ড্যাশবোর্ড স্ক্রিন (Settings ও Logout বাটন যুক্ত করা হয়েছে)
  return (
    <div className="min-h-screen bg-gray-100 p-4">
      <div className="max-w-6xl mx-auto space-y-6">
        
        {/* হেডার পার্ট - বাটন সহ */}
        <div className="bg-white rounded-3xl shadow-lg p-6 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
          <div>
            <h1 className="text-3xl font-bold">TelcoAssist AI</h1>
            <p className="text-gray-500 mt-1">Telecom & IBS Work Management Assistant</p>
          </div>

          <div className="flex items-center gap-3 w-full sm:w-auto justify-end">
            <button 
              onClick={() => setScreen('settings')}
              className="bg-gray-100 hover:bg-gray-200 px-4 py-2 rounded-2xl text-sm font-medium transition"
            >
              ⚙️ Settings
            </button>
            <div className="bg-blue-100 px-4 py-2 rounded-2xl text-sm font-medium">
              RF Operations
            </div>
          </div>
        </div>

        {/* কার্ড সেকশন */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
          {cards.map((card, index) => (
            <div key={index} className="bg-white rounded-3xl shadow-md p-5">
              <p className="text-gray-500 text-sm">{card.title}</p>
              <h2 className="text-3xl font-bold mt-2">{card.value}</h2>
            </div>
          ))}
        </div>

        {/* সাইট স্ট্যাটাস এবং কুইক অ্যাকশন */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          <div className="lg:col-span-2 bg-white rounded-3xl shadow-md p-6">
            <div className="flex items-center justify-between mb-4">
              <h2 className="text-xl font-semibold">Site Status</h2>
              <button className="bg-black text-white px-4 py-2 rounded-2xl text-sm">Add Site</button>
            </div>

            <div className="space-y-4">
              {sites.map((site, index) => (
                <div key={index} className="border rounded-2xl p-4 flex items-center justify-between">
                  <div>
                    <h3 className="font-bold text-lg">{site.id}</h3>
                    <p className="text-gray-500 text-sm">{site.region}</p>
                  </div>
                  <div>
                    <span className="bg-gray-100 px-4 py-2 rounded-xl text-sm font-medium">{site.status}</span>
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* কুইক অ্যাকশন এবং এআই */}
          <div className="bg-white rounded-3xl shadow-md p-6">
            <h2 className="text-xl font-semibold mb-4">Quick Actions</h2>
            <div className="space-y-3">
              {actions.map((action, index) => (
                <button key={index} className="w-full bg-gray-100 hover:bg-black hover:text-white transition rounded-2xl p-4 text-left font-medium">
                  {action}
                </button>
              ))}
            </div>

            <div className="mt-6 bg-black text-white rounded-3xl p-5">
              <h3 className="text-lg font-semibold">AI Assistant</h3>
              <p className="text-sm text-gray-300 mt-2">Ask anything related to telecom operations.</p>
              <div className="mt-4 bg-white rounded-2xl p-2 flex items-center">
                <input type="text" placeholder="Generate material request..." className="flex-1 outline-none text-black px-2" />
                <button className="bg-black text-white px-4 py-2 rounded-xl text-sm">Send</button>
              </div>
            </div>
          </div>
        </div>

        {/* ম্যাটেরিয়াল রিকোয়েস্ট টেবিল */}
        <div className="bg-white rounded-3xl shadow-md p-6">
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-xl font-semibold">Recent Material Requests</h2>
            <button className="bg-black text-white px-4 py-2 rounded-2xl text-sm">View All</button>
          </div>
          <div className="overflow-x-auto">
            <table className="w-full">
              <thead>
                <tr className="text-left text-gray-500 border-b">
                  <th className="pb-3">Site</th>
                  <th className="pb-3">Material</th>
                  <th className="pb-3">Qty</th>
                  <th className="pb-3">Status</th>
                </tr>
              </thead>
              <tbody>
                <tr className="border-b">
                  <td className="py-4">NWMOB1</td>
                  <td>RF Cable</td>
                  <td>140m</td>
                  <td><span className="bg-yellow-100 px-3 py-1 rounded-lg text-sm">Pending</span></td>
                </tr>
                <tr className="border-b">
                  <td className="py-4">DHKCFI</td>
                  <td>Battery</td>
                  <td>2 PCS</td>
                  <td><span className="bg-green-100 px-3 py-1 rounded-lg text-sm">Delivered</span></td>
                </tr>
                <tr>
                  <td className="py-4">DHSETI</td>
                  <td>2x4 Port</td>
                  <td>1 PCS</td>
                  <td><span className="bg-blue-100 px-3 py-1 rounded-lg text-sm">Processing</span></td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>

      </div>
    </div>
  );
}
